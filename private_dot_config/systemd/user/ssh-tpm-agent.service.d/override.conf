# Fall back to the gnome-keyring agent so one IdentityAgent serves both
# TPM sealed keys and the FIDO2 keys held by the keyring.
[Service]
ExecStart=
ExecStart=%h/.local/bin/ssh-tpm-agent -A %t/keyring/ssh
